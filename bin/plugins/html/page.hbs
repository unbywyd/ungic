<!DOCTYPE html>
<html lang="{{lang}}"{{#if rtl}} dir="rtl"{{/if}}>
<head>
    <meta charset="UTF-8">
    <meta name="description" content="{{description}}">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <un-pipe css="{{#each components}}{{.}}{{/each}}"></un-pipe>
    <title>{{title}}</title>
</head>
<body>

</body>
</html>